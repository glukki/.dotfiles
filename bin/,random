#!/usr/bin/env node

var args = process.argv.slice(2);
if (!args.length) {
  return console.log('Provide arguments to choose from');
}

var index = Math.floor(Math.random() * args.length);
console.log(args[index]);
